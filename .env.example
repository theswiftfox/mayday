# Server configuration
MYDAY_PORT=3001
MYDAY_STATIC_DIR=
RUST_LOG=myday_server=debug,tower_http=debug

# Note: Integration tokens are stored in the app's config file
# (typically ~/.config/myday/config.json)
# You can configure them via the Settings page in the UI.
